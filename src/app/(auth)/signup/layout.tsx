import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create your account | Credit Card Chris",
  description: "Start free: add your cards and always know which one to use. No bank login required.",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
